apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ingress-example
spec:
  rules:
  - host: 
    http:
     paths:
     - pathType: Prefix
       path: /
       backend:
         service:
           name: java-app-svc
           port:
             number: 8080
